# Python
bridge/__pycache__/
bridge/venv/
bridge/*.bak
bridge/*.bak2
*.pyc

# Config (contains live host list — commit a blank template instead)
# Uncomment if you don't want to track config.json:
# public/config.json

# Auth data (password hashes, LDAP config)
data/auth.json

# OS
.DS_Store
Thumbs.db
